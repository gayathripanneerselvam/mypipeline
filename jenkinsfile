pipeline {
agent any
   stages {
stage('build') {
  steps {
    sh "git clone https://github.com/kliakos/sparkjava-war-example.git /tmp/tmp2"
    sh "cd /tmp/tmp2/"
    sh "mvn clean install"
  }
}

   }
}

