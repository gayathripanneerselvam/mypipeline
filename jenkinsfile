pipeline {
agent any
   stages {
stage('build') {
  steps {
    sh "git clone https://github.com/kliakos/sparkjava-war-example.git /tmp/tmp"
    sh "cd /tmp/tmp"
    sh "mvn clean install"
  }
}

   }
}

