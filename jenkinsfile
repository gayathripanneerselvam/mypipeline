pipeline {
agent any
   stages {
stage('build') {
  steps {
    sh "git clone https://github.com/kliakos/sparkjava-war-example.git /tmp/tmp1"
    sh "cd /tmp/tmp1"
    sh "mvn clean install"
  }
}

   }
}

